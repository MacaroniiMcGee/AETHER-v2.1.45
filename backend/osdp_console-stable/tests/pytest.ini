[pytest]
log_cli = true
log_cli_level = INFO
log_file = tests/logs/pytest.log
log_file_level = DEBUG
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    manual: mark a test as requiring manual intervention
